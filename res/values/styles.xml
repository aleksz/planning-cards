<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Black">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="CardValue">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:shadowColor">#aa4213</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_centerHorizontal">true</item>
        <!-- <item name="android:textIsSelectable">false</item> -->
    </style>

    <style name="CardBackground">
        <item name="android:background">@drawable/card_background</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="CardIconBackground" parent="CardBackground">
        <item name="android:background">@drawable/card_icon_background</item>
    </style>

    <color name="CardBackground.Gradient.Start">#E37222</color>
    <color name="CardBackground.Gradient.End">#782327</color>
    <color name="CardBackground.Border">#000000</color>
    <dimen name="CardBackground.Border">2dp</dimen>

</resources>